rsync -av --delete dist/ titan.djones.co:servers/portfolio.djones.co/
docker run --rm \
  -u $(id -u):$(id -g) \
  -e HOME \
  -v $HOME:$HOME \
  -v $(pwd):/host \
  -w /host \
  tortxof/awscli \
  aws s3 sync dist s3://portfolio.djones.co --delete
